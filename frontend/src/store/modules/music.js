// import api from '../../api'

// state
const state = {
  // searchKeyword: null,
}

// getters
const getters = {

}

// mutations
const mutations = {
  // SET_SEARCH_KEYWORD(state, keyword) {
    // state.searchKeyword = keyword
  // }
}

// actions
const actions = {
  // async searchArtists(context, params) {
    // await api.searchArtists(params)
  // }
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};  